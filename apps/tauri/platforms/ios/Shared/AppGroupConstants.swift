import Foundation

/// App Group 与共享文件命名（主 App 与 Packet Tunnel 共用）。
enum AppGroupConstants {
    static let identifier = "group.com.kuayun.vpn"
    static let clashConfigFileName = "config.yaml"
    static let tunnelMetaFileName = "tunnel_meta.json"
}
