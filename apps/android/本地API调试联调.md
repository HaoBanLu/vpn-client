# Android 本地 API 调试（存档）

> **`apps/android` 已存档。** 现行客户端为 [`apps/tauri`](../tauri/)，包名 **`com.vpn.kuayun`**。

联调请改 Tauri 环境变量（如 `.env.development` / `VITE_API_BASE_URL`），见 [`跨云客户端打包说明.md`](../tauri/跨云客户端打包说明.md)。
