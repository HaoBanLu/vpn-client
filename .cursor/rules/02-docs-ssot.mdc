---
description: 文档 SSOT：功能todo、文档目录、禁止临时 md
alwaysApply: true
---

# 文档单一事实来源

## SSOT

- 功能实现状态以 [`docs/功能todo.md`](docs/功能todo.md) 为准；完成/调整/废弃须同步更新。
- 产品需求、交互、业务规则、接口约定、验收标准统一放 `docs/`。
- 文档索引见 [`docs/文档目录.md`](docs/文档目录.md)；历史归档文档勿作实现依据。

## 功能 todo 维护

每次功能变更：

- 新增 → `未开始` / `进行中`
- 完成 → `已完成` + 日期 + 验证方式
- 变更 → 更新备注或历史说明
- 废弃 → `已废弃` + 原因

## 整洁

- 禁止在仓库根目录堆临时/过时一次性 `.md` 笔记。
- 重要决策归档到 `docs/`（产品或指南），不散落根目录。
- 提交前删除不再使用的源文件与冗余资源；维护 `.gitignore`（构建二进制、Python 缓存、模拟器截图、`ssh_debug` 运行产物等）。
- 重构后清理无用依赖（`package.json` / `go.mod` / Gradle 等）。
- 协作规则变更时：同步根 [`AGENTS.md`](AGENTS.md) 索引与对应 `.cursor/rules/*.mdc`；勿再引用已删除的「旧 AGENTS 长文 §N」。
