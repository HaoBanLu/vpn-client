---
description: Android 存档目录只读提醒（发版已迁 Tauri）
globs: apps/android/**/*
alwaysApply: false
---

# Android（存档）规范

- **`apps/android` 已存档，不再维护业务。** 见 [`apps/android/ARCHIVE.md`](apps/android/ARCHIVE.md)。
- **现行 Android 发包 / 开发**：[`apps/tauri`](apps/tauri)（Vue + `src-tauri/android` VPN overlay）。
- 本目录仅允许：修 `mihomo-core` 构建断裂、签名材料路径等**构建依赖**；禁止新功能、UX、连接策略改动。
- 历史连接可信 / 重连策略样本仍可读；新实现对齐到 Tauri Android 插件与 [`docs/product/Tauri-Android功能对齐.md`](docs/product/Tauri-Android功能对齐.md)。
- 验证与发版：`cd apps/tauri && npm test` / Tag → `app-release.yml`。
